{{>layout/header}}

<div class="title">그룹을 선택해주세요</div>
<div id="home_wrapper">
{{#userGroups}}
    <div class="box">
        <a href="/schedule?groupSeq={{groupSeq}}&roleSeq={{roleSeq}}">
            <b>{{group.name}}</b>
            <p>{{role.name}}</p>
        </a>
    </div>
{{/userGroups}}
    <div class="box">
        <a href="/group"><i class="fas fa-plus"></i></a>
    </div>
</div>

{{>layout/footer}}
